kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: io-dvo-metrics-access
  namespace: deployment-validation-operator
spec:
  podSelector:
    matchLabels:
      app: deployment-validation-operator
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: openshift-insights
